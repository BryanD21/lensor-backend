{
  "name": "lensor-backend",
  "version": "1.0.0",
  "description": "Backend JSON Server para LENSOR",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "json-server --watch db.json --port 3000 --host 0.0.0.0"
  },
  "dependencies": {
    "json-server": "^0.17.4",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}